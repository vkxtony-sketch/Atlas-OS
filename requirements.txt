# Atlas OS Dependencies
# Pinned to widely-available, stable versions as of 2026.

# --- Web / HTTP ---
httpx>=0.27,<1.0

# --- Server / API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0

# --- UI ---
streamlit>=1.32,<2.0

# --- Optional: sentence-transformers for semantic knowledge search ---
# Uncomment locally only if you want embeddings.
# sentence-transformers>=2.6,<3.0

# --- Test runner (tests only) ---
pytest>=8.0

# --- Optional: PyNaCl / cryptography for at-rest encryption ---
# Already pulled in transitively by `cryptography`, but explicit for visibility.
cryptography>=42.0

# --- Local-only utility libs ---
uuid>=1.30
typing-extensions>=4.10
